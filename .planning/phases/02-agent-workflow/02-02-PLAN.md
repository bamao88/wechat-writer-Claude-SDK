---
phase: 02-agent-workflow
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Agent can search NotebookLM based on topic and retrieve materials"
    - "Agent can synthesize search results into research report"
    - "Agent can generate article outline based on research"
    - "Agent can write complete article following outline"
  artifacts: []
  key_links: []
---

<objective>
Verify end-to-end workflow produces complete article from topic.

Purpose: Validate requirements RSH-01, RSH-02, OTL-01, ART-01 with real execution.
Output: Human-verified confirmation that full workflow works.
</objective>

<execution_context>
@/Users/xpw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xpw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-workflow/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete agent workflow with production prompt:
- Loads system prompt from prompts/prompt_run.txt
- Supports multiple NotebookLM searches (2-3 rounds)
- Generates outline then full article
- Uses 8192 max_tokens for article output
  </what-built>
  <how-to-verify>
1. **Prerequisites:**
   - NotebookLM Skill: 项目内 `notebooklm_skill/` 或 NOTEBOOKLM_SKILL_DIR 指向 Skill 根目录；Skill 下已建 `.venv` 并 `pip install -r requirements.txt`；已执行一次认证：`notebooklm_skill/.venv/bin/python scripts/auth_manager.py setup`
   - .env：NOTEBOOK_URL 为有内容的笔记本链接；WECHAT_WRITER_ANTHROPIC_*（或 ANTHROPIC_*）为有效官方 API key 与模型

2. **Run with real topic:**
   ```bash
   python main.py "产品经理需要参与技术选型"
   ```
   (Or any topic related to your NotebookLM notebook content)

3. **Verify research phase:**
   - Agent makes first search call (required)
   - Agent makes second search call (required)
   - Console shows [工具] 调用 search_notebooklm... multiple times（NotebookLM Skill 通过 subprocess 调用 ask_question.py）

4. **Verify outline generation:**
   - After research, agent outputs outline structure
   - Outline has numbered sections/points

5. **Verify article output:**
   - Final output contains complete article (标题 + 正文)
   - Article is written in first person ("我")
   - Article uses materials from research (not generic)
   - Article is 1000+ characters

6. **Overall workflow:**
   - Total tool_calls >= 2 (at least 2 research rounds)
   - Process completes without errors
   - Output is coherent article on topic
  </how-to-verify>
  <resume-signal>Type "approved" if full workflow works, or describe specific phase that failed</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all success criteria.
</verification>

<success_criteria>
Phase 2 Success Criteria (from ROADMAP.md):
1. Agent can search NotebookLM based on user's topic and retrieve relevant materials
2. Agent can synthesize search results into a structured research report
3. Agent can generate an article outline based on research findings
4. Agent can write a complete article following the generated outline
5. Outline and article generation use existing prompts from `prompts/` directory
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-workflow/02-02-SUMMARY.md`
</output>
